#pragma once

// #include <utils/Entity.h>

/*
 * TODO(rafael): think a better way to make a wrapper in the filament::entity
 * that don't allocates more memory than the necessary. Sure, now is not the
 * best time to think about optimization, just to make sure ;D
 */
namespace nn {

// Alias for Filament's Entity type
// using Entity = utils::Entity;

} // namespace nn